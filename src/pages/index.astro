---
import { getCollection } from 'astro:content'
import { HomeCarousel } from '@/components/HomeCarousel'
import { SITE, } from '@/consts'
import * as React from 'react'
import Container from '@/components/Container.astro'
import { Typewriter } from "@/components/ui/typewriter"
import { StackMarquee } from '@/components/StackMarquee.tsx'
import HomeLayout from '@/layouts/HomeLayout.astro'
import { getProjectsForHomepage } from '@/lib/data.ts'
import FooterLink from '@/components/FooterLink.astro'

const featuredProjects = (await getProjectsForHomepage())

---

<HomeLayout title="Startseite" description={SITE.DESCRIPTION} hideHeader>


  <section class="relative my-24 w-full h-auto lg:h-full flex items-center justify-center bg-white py-12">
    <Container size="xl">

      <div class="flex  flex-col space-y-20 w-full">

        <div class="mx-auto md:text-4xl lg:text-5xl sm:text-3xl text-2xl flex flex-col items-start justify-start bg-background font-normal overflow-hidden">
          <p class="whitespace-pre-wrap text-center leading-tight">
            <span>Hi 👋! Ich bin <b>Danny</b>,<br/> </span>
            <Typewriter
              client:only="react"
              text={[
                "Senior Webentwickler",
                "Fullstack-Entwickler",
                "Laravel-Entwickler",
                "React-Entwickler",
                "Vue.js-Entwickler",
              ]}
              speed={140}
              className="text-[#F9B233] font-bold"
              waitTime={1500}
              deleteSpeed={40}
              cursorChar={"_"}
            /> <br/> aus <b>Bonn</b>.
          </p>
          <div class="w-full text-center flex items-center pt-6">
            <ul class="divide-x text-2xl divide-stone-400text-stone-800 flex mx-auto">
              <FooterLink>
                <a href="/ueber-mich">Über mich</a>
              </FooterLink>
              <FooterLink>
                <a href="/projekte/1">Referenzen</a>
              </FooterLink>
            </ul>
          </div>
        </div>


       <HomeCarousel client:only="react" projects={featuredProjects} />


      </div>
    </Container>
  </section>

</HomeLayout>
